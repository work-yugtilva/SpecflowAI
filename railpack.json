{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "python": "3.12"
  },
  "deploy": {
    "startCommand": "sh -c 'cd backend/src && exec uvicorn main:app --host 0.0.0.0 --port ${PORT:-8001}'"
  }
}
